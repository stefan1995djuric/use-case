import { defineMessages } from 'react-intl';

export const scope = 'location';

export default defineMessages({
  placeholder: {
    id: `${scope}.placeholder`,
    defaultMessage: 'Please enter your location',
  },
});
